# Replione V2 Rebuild

This repository is currently being rebuilt into a clean, modern architecture using FastAPI, SQLAlchemy, and a structured frontend.

## Architecture
- `backend/`: FastAPI application containing routers, schemas, models, and core logic.
- `frontend/`: Vanilla HTML, CSS, and modularized JS.
